# Kubernetes Service - Incident Service (THEORETICAL)

apiVersion: v1
kind: Service
metadata:
  name: incident-service
  labels:
    app: incident-service
spec:
  type: ClusterIP
  selector:
    app: incident-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
