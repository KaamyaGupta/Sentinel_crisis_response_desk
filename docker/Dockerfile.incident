# Dockerfile for Incident Microservice (THEORETICAL)
# --------------------------------------------------
# Build: docker build -f docker/Dockerfile.incident -t disaster-incident ./incident-service

FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

EXPOSE 3002

CMD ["node", "server.js"]
